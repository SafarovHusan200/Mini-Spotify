import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

// const root =  ReactDOM.createRobot(document.getElementById("root"));
const husan = ReactDOM.createRoot(document.getElementById("root"));

husan.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
